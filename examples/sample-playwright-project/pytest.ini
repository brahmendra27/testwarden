[pytest]
addopts = --browser chromium --screenshot only-on-failure --tracing retain-on-failure --reruns 2
testwarden_url = http://localhost:8787
testwarden_framework = pytest-playwright
; Set the project API key via env: TESTWARDEN_API_KEY (printed by the seeder)
